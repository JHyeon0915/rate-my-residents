{
    "scripts": {
        "start": "node server/index.js",
        "build": "cd rate-my-residents && npm install && npm run build"
    },
    "engines": {
        "node": "18.12.0"
    },
    "dependencies": {
        "mysql": "^2.18.1"
    }
}
