{
    "scripts": {
        "start": "node server/index.js",
        "build": "cd rate-my-residents && npm install && npm run build"
    },
    "engines": {
        "node": "18.12.0"
    },
    "dependencies": {
        "body-parser": "^1.20.1",
        "cors": "^2.8.5",
        "mysql": "^2.18.1"
    }
}
